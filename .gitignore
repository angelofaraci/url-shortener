# Dependencies
node_modules/

# Environment variables (keep the .env.example files tracked)
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# Build output
dist/
build/

# Vite
.vite/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Kubernetes secrets (real values) — commit k8s/secret.example.yaml instead
k8s/secret.yaml

# Terraform local state and working files — never commit real state to a public repo
**/.terraform/
**/terraform.tfstate
**/terraform.tfstate.backup
**/tfplan
**/*.tfvars

# Third-party installer artifacts, not project source
aws/
awscliv2.zip
kubectl
