---
- name: Template the nginx reverse-proxy configuration
  ansible.builtin.template:
    src: nginx.conf.j2
    dest: "{{ app_dir }}/nginx/nginx.conf"
    owner: root
    group: root
    mode: "0640"
  notify: Restart the proxy container
